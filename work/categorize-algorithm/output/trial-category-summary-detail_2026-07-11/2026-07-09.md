---
対象期間: 2026年07月08日 〜 2026年07月09日
作成日: 2026-07-09
形式: カテゴリ別詳細・試作版 v2（Opus 意味分類・カテゴリ内統合・複数カテゴリ掲載）
---

# Claude Code 公式ドキュメント更新サマリ（カテゴリ別詳細・試作 v2）

```markdown
今回の変更 78 項目を 64 エントリに集約。カテゴリ別の掲載数は 「アプリ・プラットフォーム」16件、「拡張機能（プラグイン・スキル・フック）」15件、「コマンド・ワークフロー」13件、「モデル・利用状況」10件、「Agent SDK・サブエージェント」9件、「セッション・コンテキスト」8件、「MCP・外部連携」8件、「パーミッション・セキュリティ」8件、「エンタープライズ基盤」6件、「設定・環境変数」5件（1 エントリが複数カテゴリに掲載される場合は重複計上）。
```

## カテゴリ別の変更点

### 🔐 パーミッション・セキュリティ — 8件

- **Desktop application › Work with code**: デスクトップの権限モードが Manual／Accept edits／Plan／Auto／Bypass permissions に整理・改称された（旧 Ask permissions・Auto accept edits・Plan mode）。また Browser ペインで外部サイトを開いて操作する機能が追加され、書き込み操作は全モードで安全性分類器の審査対象、Auto/Bypass 以外ではドメイン許可リストの確認も適用される。 — [English](https://code.claude.com/docs/en/desktop#work-with-code)
- **1ページ・2セクション**: how-claude-code-works の権限モード名が Manual／Accept edits／Plan／Auto に改称された（旧 Default・Auto-accept edits・Plan mode・Auto mode）。また `/doctor` が「インストールと設定の問題を診断し修正もできるセットアップ点検」として説明し直された。 — [how-claude-code-works](https://code.claude.com/docs/en/how-claude-code-works#stay-safe-with-checkpoints-and-permissions) / [how-claude-code-works](https://code.claude.com/docs/en/how-claude-code-works#work-effectively-with-claude-code)
- **Choose a permission mode › Switch permission modes**: permission-modes で、VS Code 拡張のモード表記が Plan mode/Auto mode から Plan/Auto に改称された。クラウドセッションは Accept edits／Plan／Auto を、ローカルの Remote Control セッションは Manual／Accept edits／Plan を提供する。Remote Control はセッションの実モードをアプリに報告するようになり（v2.1.202 以降）、Bypass permissions だけは報告されない例外も明記された。 — [English](https://code.claude.com/docs/en/permission-modes#switch-permission-modes)
- **Choose a permission mode › Analyze before you edit with plan mode**: plan モード等で auto mode が既定でブロックする対象が v2.1.205 で追加された。`~/.claude/projects/` 配下のセッション transcript（.jsonl 履歴）への書き込みと、会話中で代入されていないシェル変数やそれを起点とする glob を対象にした `rm -rf`／`Remove-Item -Recurse -Force` が、防御的にブロックされる。削除対象のリテラルパスを明示すればブロックは解ける。 — [English](https://code.claude.com/docs/en/permission-modes#analyze-before-you-edit-with-plan-mode)
- **Configure permissions › Permission modes**: permissions ページで `plan` モードの説明が更新され、CLI と VS Code 拡張では「Plan」と表示されることが明記された（旧「Plan Mode」）。 — [English](https://code.claude.com/docs/en/permissions#permission-modes)
- **Create and distribute a plugin marketplace › Marketplace schema**: プラグインマーケットプレイスの予約名に `first-party-plugins` と `healthcare` が追加された。予約名は読み込みのたびに再チェックされ、名前が予約される前に登録済みのマーケットプレイスも以後は読み込みを停止し「信頼できないソースから登録」と報告される（v2.1.205 での変更）。別名で再追加すれば再び読み込まれる。 — [English](https://code.claude.com/docs/en/plugin-marketplaces#marketplace-schema)
- **Configure the sandboxed Bash tool › Configure sandboxing**: サンドボックス化 Bash ツールの認証情報マスキング説明で、`network.tlsTerminate` の文言が「HTTPS を終端」から「TLS を終端」に更新された。あわせて設定ミスの報告先から `/doctor` が外された。 — [English](https://code.claude.com/docs/en/sandboxing#configure-sandboxing)
- **Get started with Claude Code on the web › Start a task**: web-quickstart のタスク開始手順で、モードドロップダウンの表記が更新された。既定は Accept edits で、Plan に切り替えると承認待ちになる。クラウドセッションが提供しないモードの表記が「Ask permissions」から「Manual」に改称された。 — [English](https://code.claude.com/docs/en/web-quickstart#start-a-task)

### 🧰 拡張機能（プラグイン・スキル・フック） — 15件

- **Debug your configuration › Check hooks**: フックが発火しない原因としてマッチャーの誤りが整理された。`matcher` は `|` または `,` 区切りで複数ツール名を指定でき（`,` は v2.1.191 以降）、綴り間違いは何にもマッチせず無言で失敗する。配列値はスキーマエラーとなり、通常の設定ファイルはファイル全体が拒否されるが、managed settings では無効なエントリのみが除去され他のフックは有効なままとなる。 — [English](https://code.claude.com/docs/en/debug-your-config#check-hooks)
- **Error reference › Find your error**: エラーリファレンスの索引表に、`Download timed out`（インストールエラー）、`--json-schema is not a valid JSON Schema`（コマンドラインエラー）、`Could not import <server>`（Claude Desktop からのインポート）、`Marketplace ... registered from an untrusted source`（プラグインエラー）の行が追加された。 — [English](https://code.claude.com/docs/en/errors#find-your-error)
- **Error reference › Command-line errors**: エラーリファレンスのコマンドラインエラー節が拡充された。`--json-schema` が JSON Schema として無効なら終了コード1で失敗し（`format` キーワードは注釈として受理）、`claude mcp add-from-claude-desktop` は名前が無効なサーバを報告しつつ他のサーバは取り込む。v2.1.205 より前は無効スキーマが無言で無視され、最初の失敗でインポート全体が止まっていた。 — [English](https://code.claude.com/docs/en/errors#command-line-errors)
- **Run Claude Code programmatically › Examples**: headless（プログラム実行）の説明が更新された。無効な JSON Schema は `claude` がエラー終了し（`format` は注釈扱い）、`system/init` イベントに実装済みプロトコル挙動を示す `capabilities` 配列（例 `interrupt_receipt_v1`）が追加された。`-p` モードで `/model`・`/effort`・`/fast`・`/color`・`/rename` が引数付きで、`/mcp` が状態のテキスト要約として使える（v2.1.205 以降）。 — [English](https://code.claude.com/docs/en/headless#examples)
- **1ページ・2セクション**: プラグイン依存関係の説明が更新され、依存の問題や制約付き依存の更新スキップの表示先から `/doctor` が外され、`claude plugin list` と `/plugin` インターフェイス（Errors タブ）に集約された。制約を満たすタグが無い依存はスキップされ、制約元プラグイン名が示される。 — [plugin-dependencies](https://code.claude.com/docs/en/plugin-dependencies#how-constraints-interact) / [plugin-dependencies](https://code.claude.com/docs/en/plugin-dependencies#resolve-dependency-errors)
- **Create and distribute a plugin marketplace › Marketplace schema**: プラグインマーケットプレイスの予約名に `first-party-plugins` と `healthcare` が追加された。予約名は読み込みのたびに再チェックされ、名前が予約される前に登録済みのマーケットプレイスも以後は読み込みを停止し「信頼できないソースから登録」と報告される（v2.1.205 での変更）。別名で再追加すれば再び読み込まれる。 — [English](https://code.claude.com/docs/en/plugin-marketplaces#marketplace-schema)
- **Claude Code settings › Settings files**: 設定ページに項目が追加・更新された。managed settings 専用の `browserExternalPageTools`、`disableBundledSkills`（`/doctor` は例外で typable のまま・`DISABLE_DOCTOR_COMMAND` で非表示化）、`skillListingBudgetFraction` の挙動説明が更新され、設定ファイルのエラー詳細を見るコマンドが `/doctor` から `claude doctor` に変わった。 — [English](https://code.claude.com/docs/en/settings#settings-files)
- **Extend Claude with skills › Bundled skills**: skills のバンドルスキル一覧に `/doctor` が加わった。v2.1.205 以降、`/doctor` セットアップ点検はプロンプトベースのバンドルスキルとなり、`disableBundledSkills` の唯一の例外として typable のまま残る（非表示化は `DISABLE_DOCTOR_COMMAND` か `skillOverrides` の `"doctor":"off"`）。 — [English](https://code.claude.com/docs/en/skills#bundled-skills)
- **Extend Claude with skills › Troubleshooting**: skills のトラブルシュートで、スキル一覧の説明が予算（モデルコンテキストの1%）を超えると使用頻度の低いスキルの説明から削られる仕組みが整理された。`/doctor` は一覧のコンテキストコストの見積りを示し、超過時はデバッグログにも警告が書かれる。`/context` の Skills 行は予算適用後のサイズを示す（v2.1.196 以降）。 — [English](https://code.claude.com/docs/en/skills#troubleshooting)
- **Run parallel sessions with worktrees › Start Claude in a worktree**: worktrees で、起動時にワークツリーディレクトリへ入れない場合（`WorktreeCreate` フックがディレクトリ以外を出力した、削除された等）に、パスを示すエラーを出して終了コード1で終わるようになった（v2.1.205 より前はセッションがクラッシュし、`-p` では約30秒スタール後にコード0で終了していた）。 — [English](https://code.claude.com/docs/en/worktrees#start-claude-in-a-worktree)
- **Environment variables › Variables**: 環境変数が更新された。`CLAUDE_CODE_DISABLE_BUNDLED_SKILLS`（`/doctor` は例外で typable のまま）、サブエージェントのシステムプロンプト追記を有効化する `CLAUDE_CODE_ENABLE_APPEND_SUBAGENT_PROMPT`（v2.1.205 以降・フラグが自動設定）、`/doctor` スキルとその `/checkup` エイリアスを隠す `DISABLE_DOCTOR_COMMAND` の説明が更新された。 — [English](https://code.claude.com/docs/en/env-vars#variables)
- **Hooks reference › Hook events**: hooks リファレンスが更新された。`ExitPlanMode` の `allowedPrompts` が非推奨・無視となり、代わりに `plan`・`planFilePath` がフックへ注入される。`WorktreeCreate` フックの戻り値の扱いが整理され（相対パス許容・ANSI 除去・stdout 最終行）、入れないパスはセッションがエラーを出し終了コード1で終わる（v2.1.205 より前はクラッシュ、`-p` では約30秒スタール後コード0）。 — [English](https://code.claude.com/docs/en/hooks#hook-events)
- **Plugins reference › Plugin components reference**: プラグインリファレンスの LSP サーバ設定に `shutdownTimeout` と `restartOnCrash` が追加された（v2.1.205 以降・従来は設定するとサーバがスキップされた）。同一拡張子を複数サーバが宣言した場合は最初に登録されたものが処理し、初期化に失敗したサーバはスキップされ拡張子を占有しなくなった。 — [English](https://code.claude.com/docs/en/plugins-reference#plugin-components-reference)
- **Plugins reference › Plugin manifest schema**: プラグインリファレンスで、既定フォルダとマニフェストキーが両方ある場合に無視されるフォルダの警告表示先が更新された。`/doctor` が外れ、`claude plugin list` と `/plugin` 詳細ビューで警告される（v2.1.140 以降）。 — [English](https://code.claude.com/docs/en/plugins-reference#plugin-manifest-schema)
- **Customize your status line › Subagent status lines**: ステータスラインのサブエージェント行入力に、各タスクの解決済みモデルID `model` とそのコンテキストウィンドウサイズ `contextWindowSize` が追加された（v2.1.205 以降・モデル未解決のタスクでは省略）。これにより `tokenCount` から行ごとの使用率を描画できる。 — [English](https://code.claude.com/docs/en/statusline#subagent-status-lines)

### 🔌 MCP・外部連携 — 8件

- **Claude Code changelog**: changelog に v2.1.206（2026年7月9日）が追加された。`/cd` のパス補完、`CLAUDE.md` の削減を提案する `/doctor` チェック、`/commit-push-pr` の push リモート自動許可、`--mcp-config` 等での per-server `request_timeout_ms` 無視の修正、OAuth MCP サーバの再認証不具合の修正、`/model` ピッカーの価格表示不具合の修正、Bedrock 起動ハングの修正など多数の追加・修正が記載された。 — [English](https://code.claude.com/docs/en/changelog)
- **Error reference › Find your error**: エラーリファレンスの索引表に、`Download timed out`（インストールエラー）、`--json-schema is not a valid JSON Schema`（コマンドラインエラー）、`Could not import <server>`（Claude Desktop からのインポート）、`Marketplace ... registered from an untrusted source`（プラグインエラー）の行が追加された。 — [English](https://code.claude.com/docs/en/errors#find-your-error)
- **Error reference › Command-line errors**: エラーリファレンスのコマンドラインエラー節が拡充された。`--json-schema` が JSON Schema として無効なら終了コード1で失敗し（`format` キーワードは注釈として受理）、`claude mcp add-from-claude-desktop` は名前が無効なサーバを報告しつつ他のサーバは取り込む。v2.1.205 より前は無効スキーマが無言で無視され、最初の失敗でインポート全体が止まっていた。 — [English](https://code.claude.com/docs/en/errors#command-line-errors)
- **Error reference › Responses seem lower quality than usual**: 「応答品質が低い」トラブルシュートで、肥大化した `CLAUDE.md` や MCP ツール定義への対処が更新された。`/doctor` の点検が過大なメモリファイルや未使用拡張を検出し、`/context` が MCP ツールのトークン使用量を示す。v2.1.205 より前は `/doctor` が診断画面を開いてメモリファイルやサブエージェント定義を指摘していた。 — [English](https://code.claude.com/docs/en/errors#responses-seem-lower-quality-than-usual)
- **Connect Claude Code to tools via MCP › Installing MCP servers**: MCP で予約サーバ名が拡張された。`workspace`・`claude-in-chrome`・`computer-use`・`Claude Preview`・`Claude Browser` が組み込みサーバ用に予約され、設定で使うと読み込み時にスキップされ警告が出る。また接続に失敗したサーバの接続エラーが（ツール検索経由で）Claude に伝わり、応答で接続失敗を報告できるようになった。Bedrock 等ツール検索のない構成では従来どおり伝わらない。 — [English](https://code.claude.com/docs/en/mcp#installing-mcp-servers)
- **Connect Claude Code to tools via MCP › Import MCP servers from Claude Desktop**: Claude Desktop からの MCP サーバインポートで、`claude mcp` が許すサーバ名は英数字・ハイフン・アンダースコアのみのため、空白等を含む名前は取り込めないことが明記された。無効な名前は報告しつつ他の選択サーバは取り込む（v2.1.205 より前は最初の無効名でインポート全体が停止していた）。 — [English](https://code.claude.com/docs/en/mcp#import-mcp-servers-from-claude-desktop)
- **1ページ・3セクション**: トラブルシューティングページで `/doctor` の案内が更新された。`/doctor` はインストール・設定・拡張・コンテキスト使用状況を点検し確認後に修正を適用でき、`claude` が起動しない場合は端末から `claude doctor` を実行、MCP サーバの状態は `/mcp` で確認する旨に整理された。Search を OK 表示する例の文言も `claude doctor` に更新された。 — [troubleshooting](https://code.claude.com/docs/en/troubleshooting) / [troubleshooting](https://code.claude.com/docs/en/troubleshooting#performance-and-stability) / [troubleshooting](https://code.claude.com/docs/en/troubleshooting#get-more-help)
- **Agent SDK reference - TypeScript › Message Types**: Agent SDK（TypeScript）の型が拡張された。`SDKSystemMessage` に実装済みプロトコル挙動を示す `capabilities` 配列（例 `interrupt_receipt_v1`）が追加され、`peer` メッセージに送信者表示名 `name` と復号済み本文 `body` が加わった（いずれも v2.1.205 以降）。`name` は制御文字除去・トリム・64コードポイント上限で正規化される。 — [English](https://code.claude.com/docs/en/agent-sdk/typescript#message-types)

### 🧩 Agent SDK・サブエージェント — 9件

- **Manage multiple agents with agent view › How background sessions are hosted**: `claude attach` が、更新・ストール・移行でセッションが再起動中のときに失敗せず、待機理由を示すステータス行を出して最大約60秒まで置換プロセスを待つようになった。あわせて `/doctor` から `claude daemon status` 相当のサマリ表示が外された。 — [English](https://code.claude.com/docs/en/agent-view#how-background-sessions-are-hosted)
- **Create custom subagents › Configure subagents**: サブエージェント設定で、同一 `.claude/agents/` ディレクトリ（サブフォルダ含む）内で `name` が重複すると読み込み順で1つだけ読まれることが明記され、`/doctor` 点検が重複を報告して名前変更・削除を提案する。非対話モード用に、全サブエージェント（ネスト含む）のシステムプロンプト末尾へ追記する `--append-subagent-system-prompt` フラグが追加された（v2.1.205 以降）。 — [English](https://code.claude.com/docs/en/sub-agents#configure-subagents)
- **Create custom subagents › Work with subagents**: サブエージェントの再開挙動が明確化された。再開は同じ ID で新たな実行を開始するため、失敗・完了済みのサブエージェントもタスク一覧や Agent SDK のタスクイベントで再び running と表示される（v2.1.205 より前は以前の失敗・完了状態を表示し続けていた）。 — [English](https://code.claude.com/docs/en/sub-agents#work-with-subagents)
- **How the agent loop works › Control how the loop runs**: Agent SDK のエージェントループで、ストリーミング入力時にターン実行中へ送ったメッセージが、そのターンが max-turns 上限で終わってもキューに残り、独自の max-turns 上限で新しいターンを開始するようになった（v2.1.205 より前は終了ターンに取り込まれ、モデルに届かず失われることがあった）。 — [English](https://code.claude.com/docs/en/agent-sdk/agent-loop#control-how-the-loop-runs)
- **Get structured output from agents › Output format configuration**: Agent SDK の構造化出力で、無効な JSON Schema は起動時に問題を示すエラーで実行が失敗するようになった（v2.1.205 より前は無言で無視され非構造化テキストを返していた）。`format` キーワードは注釈として受理され強制はされない（従来は `format` を含むと無効扱い）。 — [English](https://code.claude.com/docs/en/agent-sdk/structured-outputs#output-format-configuration)
- **Agent SDK reference - TypeScript › Types**: Agent SDK（TypeScript）で `interrupt()` の戻り値が更新された。CLI が `interrupt_receipt_v1` capability を広告する場合、割り込みを生き延びたキュー内メッセージの UUID を列挙する `SDKControlInterruptResponse`（`still_queued`）を解決する。旧 CLI（v2.1.205 未満）では `undefined` を解決する。 — [English](https://code.claude.com/docs/en/agent-sdk/typescript#types)
- **Agent SDK reference - TypeScript › Message Types**: Agent SDK（TypeScript）の型が拡張された。`SDKSystemMessage` に実装済みプロトコル挙動を示す `capabilities` 配列（例 `interrupt_receipt_v1`）が追加され、`peer` メッセージに送信者表示名 `name` と復号済み本文 `body` が加わった（いずれも v2.1.205 以降）。`name` は制御文字除去・トリム・64コードポイント上限で正規化される。 — [English](https://code.claude.com/docs/en/agent-sdk/typescript#message-types)
- **Agent SDK reference - TypeScript › Tool Input Types**: Agent SDK（TypeScript）の `ExitPlanMode` ツール入力で、`allowedPrompts` フィールドが非推奨・無視となった（既存の呼び出しや transcript の検証のため受理は継続）。v2.1.205 より前は plan 実装用のプロンプトベース Bash 権限を要求していた。 — [English](https://code.claude.com/docs/en/agent-sdk/typescript#tool-input-types)
- **CLI reference › CLI flags**: CLI フラグが更新された。`--append-subagent-system-prompt`（`-p` 時のみ、全サブエージェントのシステムプロンプト末尾へ追記、v2.1.205 以降）が追加され、`--json-schema` は無効スキーマでエラー終了し `format` を注釈として受理、`--max-turns` は `stream-json` 入力時に上限で終わったターンの後続メッセージをキューに残して独自の上限で実行することが明記された。 — [English](https://code.claude.com/docs/en/cli-reference#cli-flags)

### ⚙️ 設定・環境変数 — 5件

- **1ページ・2セクション**: debug-your-config で `/doctor` の説明が、読み取り専用の診断画面から「確認後に修正を適用できるセットアップ点検」へ更新された。無効な設定ファイルや重複インストール・未使用拡張を報告して修正を提案する。端末から `claude doctor` を実行すると、セッションを開始せず読み取り専用の診断のみを出力する。 — [debug-your-config](https://code.claude.com/docs/en/debug-your-config#see-what-loaded-into-context) / [debug-your-config](https://code.claude.com/docs/en/debug-your-config#check-resolved-settings)
- **Debug your configuration › Check hooks**: フックが発火しない原因としてマッチャーの誤りが整理された。`matcher` は `|` または `,` 区切りで複数ツール名を指定でき（`,` は v2.1.191 以降）、綴り間違いは何にもマッチせず無言で失敗する。配列値はスキーマエラーとなり、通常の設定ファイルはファイル全体が拒否されるが、managed settings では無効なエントリのみが除去され他のフックは有効なままとなる。 — [English](https://code.claude.com/docs/en/debug-your-config#check-hooks)
- **Model configuration › Available models**: モデル設定で、非対話（`-p`）モードで `/model` により設定したモデルは現在のセッション限定で既定には保存されないことが追記された。プロジェクト・managed settings や組織既定モデルは引き続き優先され、次回起動時に再適用される。 — [English](https://code.claude.com/docs/en/model-config#available-models)
- **Claude Code settings › Settings files**: 設定ページに項目が追加・更新された。managed settings 専用の `browserExternalPageTools`、`disableBundledSkills`（`/doctor` は例外で typable のまま・`DISABLE_DOCTOR_COMMAND` で非表示化）、`skillListingBudgetFraction` の挙動説明が更新され、設定ファイルのエラー詳細を見るコマンドが `/doctor` から `claude doctor` に変わった。 — [English](https://code.claude.com/docs/en/settings#settings-files)
- **Environment variables › Variables**: 環境変数が更新された。`CLAUDE_CODE_DISABLE_BUNDLED_SKILLS`（`/doctor` は例外で typable のまま）、サブエージェントのシステムプロンプト追記を有効化する `CLAUDE_CODE_ENABLE_APPEND_SUBAGENT_PROMPT`（v2.1.205 以降・フラグが自動設定）、`/doctor` スキルとその `/checkup` エイリアスを隠す `DISABLE_DOCTOR_COMMAND` の説明が更新された。 — [English](https://code.claude.com/docs/en/env-vars#variables)

### 🤖 モデル・利用状況 — 10件

- **1ページ・2セクション**: エージェントビューの行サマリが、生の tool 呼び出しや `done/total` カウントではなく、セッション自身の1行レポート（64桁で切り詰め）を表示するようになった。ディレクトリでグループ化した行は色付きの状態語で始まり、peek パネルは完全なステータス文と待機中の質問を先頭に表示する。`gh` でのPRの編集・コメント・クローズ・ready 化もセッションにリンクされ、読めるテキストのないターンは状態を維持するなどの変更が v2.1.205 で入り、バージョン履歴にも記録された。 — [agent-view](https://code.claude.com/docs/en/agent-view#monitor-sessions-with-agent-view) / [agent-view](https://code.claude.com/docs/en/agent-view#version-history)
- **Claude Code changelog**: changelog に v2.1.206（2026年7月9日）が追加された。`/cd` のパス補完、`CLAUDE.md` の削減を提案する `/doctor` チェック、`/commit-push-pr` の push リモート自動許可、`--mcp-config` 等での per-server `request_timeout_ms` 無視の修正、OAuth MCP サーバの再認証不具合の修正、`/model` ピッカーの価格表示不具合の修正、Bedrock 起動ハングの修正など多数の追加・修正が記載された。 — [English](https://code.claude.com/docs/en/changelog)
- **Error reference › Request errors**: エラーリファレンスで、モデルファミリエイリアス（`opus`/`sonnet`/`haiku`/`fable`）の扱いが説明された。Anthropic API と Claude Platform on AWS では、組織と `availableModels` 許可リストが許す最新版に解決され、全バージョンが制限されている場合のみ `/model <alias>` が拒否される。v2.1.205 より前は最新版のみで判定されていた。 — [English](https://code.claude.com/docs/en/errors#request-errors)
- **Error reference › Responses seem lower quality than usual**: 「応答品質が低い」トラブルシュートで、肥大化した `CLAUDE.md` や MCP ツール定義への対処が更新された。`/doctor` の点検が過大なメモリファイルや未使用拡張を検出し、`/context` が MCP ツールのトークン使用量を示す。v2.1.205 より前は `/doctor` が診断画面を開いてメモリファイルやサブエージェント定義を指摘していた。 — [English](https://code.claude.com/docs/en/errors#responses-seem-lower-quality-than-usual)
- **1ページ・2セクション**: fast mode の説明が更新された。対話セッションで有効化した fast mode はセッション間で維持されるが、非対話（`-p`）モードでは `--settings '{"fastMode": true}'` で起動したセッションでのみ `/fast` が機能し、そのセッション限定で既定には保存されない。管理者は `fastModePerSessionOptIn` でセッションごとの opt-in を強制できる。 — [fast-mode](https://code.claude.com/docs/en/fast-mode#toggle-fast-mode) / [fast-mode](https://code.claude.com/docs/en/fast-mode#requirements)
- **Model configuration › Available models**: モデル設定で、非対話（`-p`）モードで `/model` により設定したモデルは現在のセッション限定で既定には保存されないことが追記された。プロジェクト・managed settings や組織既定モデルは引き続き優先され、次回起動時に再適用される。 — [English](https://code.claude.com/docs/en/model-config#available-models)
- **Model configuration › Restrict model selection**: モデル選択制限の説明が刷新された。Anthropic API と Claude Platform on AWS では、ファミリエイリアスが許可リストの許す最新版に解決され（例 `["sonnet","claude-opus-4-6"]` で `/model opus` は Opus 4.6 を選択）、置換通知が出る。Bedrock/Agent Platform/Foundry/Mantle はプロバイダ固有IDのため従来の拒否・置換動作に従う。フォールバックチェーンや plan モード昇格・fast mode も許可リストで同様にチェックされる。v2.1.205 より前は最新版のみで判定されていた。 — [English](https://code.claude.com/docs/en/model-config#restrict-model-selection)
- **Model configuration › Special model behavior**: モデルの特殊挙動が更新された。`availableModels` が最新 Opus を除外し旧版を許す場合、`opusplan` は許可された最新 Opus を plan モードに使い、全 Opus 除外時のみ Sonnet に留まる（Haiku→Sonnet も同様）。この旧版置換は Anthropic API と Claude Platform on AWS のみで、プロバイダ固有ID環境ではセッションのモデルに留まる。また非対話 `/effort` はセッション限定で、Fable 5・Opus 4.8・Opus 4.7 のモデル既定 effort ホールドは解除できず `Not applied` となる。 — [English](https://code.claude.com/docs/en/model-config#special-model-behavior)
- **Monitoring › Configuration details**: 監視（monitoring）ページのテレメトリ対象一覧で、`/doctor` output が `/status` output に置き換えられた。 — [English](https://code.claude.com/docs/en/monitoring-usage#configuration-details)
- **Customize your status line › Subagent status lines**: ステータスラインのサブエージェント行入力に、各タスクの解決済みモデルID `model` とそのコンテキストウィンドウサイズ `contextWindowSize` が追加された（v2.1.205 以降・モデル未解決のタスクでは省略）。これにより `tokenCount` から行ごとの使用率を描画できる。 — [English](https://code.claude.com/docs/en/statusline#subagent-status-lines)

### 🏢 エンタープライズ基盤 — 6件

- **Desktop application › Coming from the CLI?**: デスクトップの「CLI からの移行」比較表が更新された。権限モード表記が Manual/Accept edits/Plan に改称され、サードパーティプロバイダの案内で研究プレビュー名「Cowork on 3P」が「Claude Desktop on 3P」に変更された。ターミナルダイアログ型コマンド（`/permissions`・`/config` 等）は Code タブで利用不可だが、`/config key=value` は使える点も明記された。 — [English](https://code.claude.com/docs/en/desktop#coming-from-the-cli)
- **1ページ・2セクション**: エラーリファレンスのネットワーク・インストール項目が更新された。SSL 証明書エラーの案内が `/doctor` から `claude doctor` に変わり、10分以内に完了しないダウンロードは `Download timed out: exceeded the total deadline` として失敗し再試行されないことが追記された（旧版は汎用の `timeout of 600000ms exceeded`）。 — [errors](https://code.claude.com/docs/en/errors#network-and-connection-errors) / [errors](https://code.claude.com/docs/en/errors#installation-errors)
- **Feature availability › Availability by model provider**: feature-availability のプロバイダ別可用性で、デスクトップの案内にある研究プレビュー名が「Cowork on 3P」から「Claude Desktop on 3P」に変更された。 — [English](https://code.claude.com/docs/en/feature-availability#availability-by-model-provider)
- **Connect Claude Code to an LLM gateway › Configure each surface**: LLM ゲートウェイ接続で、デスクトップアプリがゲートウェイのルーティングを管理者配布の構成から読む旨のリンク先が `cowork/3p` から `third-party/claude-desktop` に変更された。また機能復旧時に外すべき認証情報変数を示すのが `/doctor` から `claude doctor` の Remote Control 節に変わった。 — [English](https://code.claude.com/docs/en/llm-gateway-connect#configure-each-surface)
- **Roll out an LLM gateway for your organization › Rollout steps**: LLM ゲートウェイのロールアウト手順で、デスクトップの MDM 配布サードパーティ推論構成に関するドキュメントのリンク先が `cowork/3p` から `third-party/claude-desktop` に更新された。 — [English](https://code.claude.com/docs/en/llm-gateway-rollout#rollout-steps)
- **Configure the sandboxed Bash tool › Configure sandboxing**: サンドボックス化 Bash ツールの認証情報マスキング説明で、`network.tlsTerminate` の文言が「HTTPS を終端」から「TLS を終端」に更新された。あわせて設定ミスの報告先から `/doctor` が外された。 — [English](https://code.claude.com/docs/en/sandboxing#configure-sandboxing)

### 🖥 アプリ・プラットフォーム — 16件

- **Use Claude Code on the web › Work with sessions**: Claude Code on the web のクラウドセッションで、`/model`・`/effort`・`/fast`・`/color`・`/rename` が引数付き（例 `/model sonnet`）で使えるようになり、`/config` も `key=value` で設定できるようになった。従来は対話ピッカーを開くコマンドは利用不可とされていた。引数形式には v2.1.205 以降が必要。 — [English](https://code.claude.com/docs/en/claude-code-on-the-web#work-with-sessions)
- **1ページ・3セクション**: デスクトップアプリの「preview / embedded browser」が「Browser ペイン」に改称され、外部サイトも開けるタブ型ブラウザとして整理された。ペイン構成（chat/diff/browser/terminal/file 等）やキーボードショートカット一覧も更新され、Browser トグル（Cmd/Ctrl+Shift+B）やモデル・effort・権限モードメニューのショートカットが追加された。サーバ起動設定のドロップダウン名も Preview から server dropdown に変わった。 — [desktop](https://code.claude.com/docs/en/desktop) / [desktop](https://code.claude.com/docs/en/desktop#arrange-your-workspace) / [desktop](https://code.claude.com/docs/en/desktop#extend-claude-code)
- **Desktop application › Work with code**: デスクトップの権限モードが Manual／Accept edits／Plan／Auto／Bypass permissions に整理・改称された（旧 Ask permissions・Auto accept edits・Plan mode）。また Browser ペインで外部サイトを開いて操作する機能が追加され、書き込み操作は全モードで安全性分類器の審査対象、Auto/Bypass 以外ではドメイン許可リストの確認も適用される。 — [English](https://code.claude.com/docs/en/desktop#work-with-code)
- **Desktop application › Enterprise configuration**: デスクトップのエンタープライズ設定に `browserExternalPageTools` が追加された。`"disabled"` にすると Claude が Browser ペインで外部ページを読んだり操作したりするツールを使えなくなる（利用者自身の外部サイト閲覧やローカル開発サーバのプレビューには影響しない）。 — [English](https://code.claude.com/docs/en/desktop#enterprise-configuration)
- **Desktop application › Coming from the CLI?**: デスクトップの「CLI からの移行」比較表が更新された。権限モード表記が Manual/Accept edits/Plan に改称され、サードパーティプロバイダの案内で研究プレビュー名「Cowork on 3P」が「Claude Desktop on 3P」に変更された。ターミナルダイアログ型コマンド（`/permissions`・`/config` 等）は Code タブで利用不可だが、`/config key=value` は使える点も明記された。 — [English](https://code.claude.com/docs/en/desktop#coming-from-the-cli)
- **1ページ・2セクション**: デスクトップアプリのクイックスタートで、既定の権限モードが「Ask permissions」から「Manual」へ改称され、Accept edits・Plan の説明も更新された。プレビュー機能の記述も Browser ペインに合わせて改められた。 — [desktop-quickstart](https://code.claude.com/docs/en/desktop-quickstart#start-your-first-session) / [desktop-quickstart](https://code.claude.com/docs/en/desktop-quickstart#now-what)
- **Feature availability › Availability by model provider**: feature-availability のプロバイダ別可用性で、デスクトップの案内にある研究プレビュー名が「Cowork on 3P」から「Claude Desktop on 3P」に変更された。 — [English](https://code.claude.com/docs/en/feature-availability#availability-by-model-provider)
- **Connect Claude Code to an LLM gateway › Configure each surface**: LLM ゲートウェイ接続で、デスクトップアプリがゲートウェイのルーティングを管理者配布の構成から読む旨のリンク先が `cowork/3p` から `third-party/claude-desktop` に変更された。また機能復旧時に外すべき認証情報変数を示すのが `/doctor` から `claude doctor` の Remote Control 節に変わった。 — [English](https://code.claude.com/docs/en/llm-gateway-connect#configure-each-surface)
- **Roll out an LLM gateway for your organization › Rollout steps**: LLM ゲートウェイのロールアウト手順で、デスクトップの MDM 配布サードパーティ推論構成に関するドキュメントのリンク先が `cowork/3p` から `third-party/claude-desktop` に更新された。 — [English](https://code.claude.com/docs/en/llm-gateway-rollout#rollout-steps)
- **Connect Claude Code to tools via MCP › Installing MCP servers**: MCP で予約サーバ名が拡張された。`workspace`・`claude-in-chrome`・`computer-use`・`Claude Preview`・`Claude Browser` が組み込みサーバ用に予約され、設定で使うと読み込み時にスキップされ警告が出る。また接続に失敗したサーバの接続エラーが（ツール検索経由で）Claude に伝わり、応答で接続失敗を報告できるようになった。Bedrock 等ツール検索のない構成では従来どおり伝わらない。 — [English](https://code.claude.com/docs/en/mcp#installing-mcp-servers)
- **Platforms and integrations › Where to run Claude Code**: platforms ページの実行環境の説明で、デスクトップでサードパーティプロバイダを使う際の研究プレビュー名が「Cowork on 3P」から「Claude Desktop on 3P」に変更された。 — [English](https://code.claude.com/docs/en/platforms#where-to-run-claude-code)
- **Continue local sessions from any device with Remote Control › Limitations**: Remote Control の制限が更新された。`/plugin` や `/resume` など端末専用コマンドはローカル CLI のみで動作し、`/model`・`/effort`・`/fast`・`/color`・`/rename` は引数付き（例 `/model sonnet`）でモバイル・Web から使えることが明記された。 — [English](https://code.claude.com/docs/en/remote-control#limitations)
- **Troubleshoot installation and login › Common installation issues**: インストールのトラブルシュートで、対応プラットフォームのプリビルドバイナリ一覧に加え、FreeBSD ではインストーラが非対応として報告することが追記された（v2.1.205 より前は Linux 扱いで実行不能なバイナリを取得していた）。 — [English](https://code.claude.com/docs/en/troubleshoot-install#common-installation-issues)
- **Use Claude Code in VS Code › Use the prompt box**: VS Code の Plan モードの表記が「Plan mode」から「Plan」に改称された（説明内容は同一）。 — [English](https://code.claude.com/docs/en/vs-code#use-the-prompt-box)
- **Get started with Claude Code on the web › Compare ways to run Claude Code**: web-quickstart の実行方法比較表が更新され、Remote Control 列の権限モード表記が「Ask, Auto accept edits, Plan」から「Manual, Accept edits, Plan」に改称された。 — [English](https://code.claude.com/docs/en/web-quickstart#compare-ways-to-run-claude-code)
- **Get started with Claude Code on the web › Start a task**: web-quickstart のタスク開始手順で、モードドロップダウンの表記が更新された。既定は Accept edits で、Plan に切り替えると承認待ちになる。クラウドセッションが提供しないモードの表記が「Ask permissions」から「Manual」に改称された。 — [English](https://code.claude.com/docs/en/web-quickstart#start-a-task)

### 📚 セッション・コンテキスト — 8件

- **1ページ・2セクション**: エージェントビューの行サマリが、生の tool 呼び出しや `done/total` カウントではなく、セッション自身の1行レポート（64桁で切り詰め）を表示するようになった。ディレクトリでグループ化した行は色付きの状態語で始まり、peek パネルは完全なステータス文と待機中の質問を先頭に表示する。`gh` でのPRの編集・コメント・クローズ・ready 化もセッションにリンクされ、読めるテキストのないターンは状態を維持するなどの変更が v2.1.205 で入り、バージョン履歴にも記録された。 — [agent-view](https://code.claude.com/docs/en/agent-view#monitor-sessions-with-agent-view) / [agent-view](https://code.claude.com/docs/en/agent-view#version-history)
- **Manage multiple agents with agent view › How background sessions are hosted**: `claude attach` が、更新・ストール・移行でセッションが再起動中のときに失敗せず、待機理由を示すステータス行を出して最大約60秒まで置換プロセスを待つようになった。あわせて `/doctor` から `claude daemon status` 相当のサマリ表示が外された。 — [English](https://code.claude.com/docs/en/agent-view#how-background-sessions-are-hosted)
- **Configure auto mode › Define trusted infrastructure**: auto mode の信頼インフラ判定で、リポジトリの公開判定に使う証拠が「分類器が読めるもの（利用者のメッセージや Claude が実行するコマンド）」に限定されることが明確化された。`gh repo view` の出力自体は分類器に届かないため、公開の根拠にはならない。 — [English](https://code.claude.com/docs/en/auto-mode-config#define-trusted-infrastructure)
- **1ページ・3セクション**: デスクトップアプリの「preview / embedded browser」が「Browser ペイン」に改称され、外部サイトも開けるタブ型ブラウザとして整理された。ペイン構成（chat/diff/browser/terminal/file 等）やキーボードショートカット一覧も更新され、Browser トグル（Cmd/Ctrl+Shift+B）やモデル・effort・権限モードメニューのショートカットが追加された。サーバ起動設定のドロップダウン名も Preview から server dropdown に変わった。 — [desktop](https://code.claude.com/docs/en/desktop) / [desktop](https://code.claude.com/docs/en/desktop#arrange-your-workspace) / [desktop](https://code.claude.com/docs/en/desktop#extend-claude-code)
- **1ページ・2セクション**: fast mode の説明が更新された。対話セッションで有効化した fast mode はセッション間で維持されるが、非対話（`-p`）モードでは `--settings '{"fastMode": true}'` で起動したセッションでのみ `/fast` が機能し、そのセッション限定で既定には保存されない。管理者は `fastModePerSessionOptIn` でセッションごとの opt-in を強制できる。 — [fast-mode](https://code.claude.com/docs/en/fast-mode#toggle-fast-mode) / [fast-mode](https://code.claude.com/docs/en/fast-mode#requirements)
- **1ページ・2セクション**: how-claude-code-works の権限モード名が Manual／Accept edits／Plan／Auto に改称された（旧 Default・Auto-accept edits・Plan mode・Auto mode）。また `/doctor` が「インストールと設定の問題を診断し修正もできるセットアップ点検」として説明し直された。 — [how-claude-code-works](https://code.claude.com/docs/en/how-claude-code-works#stay-safe-with-checkpoints-and-permissions) / [how-claude-code-works](https://code.claude.com/docs/en/how-claude-code-works#work-effectively-with-claude-code)
- **Choose a permission mode › Analyze before you edit with plan mode**: plan モード等で auto mode が既定でブロックする対象が v2.1.205 で追加された。`~/.claude/projects/` 配下のセッション transcript（.jsonl 履歴）への書き込みと、会話中で代入されていないシェル変数やそれを起点とする glob を対象にした `rm -rf`／`Remove-Item -Recurse -Force` が、防御的にブロックされる。削除対象のリテラルパスを明示すればブロックは解ける。 — [English](https://code.claude.com/docs/en/permission-modes#analyze-before-you-edit-with-plan-mode)
- **1ページ・2セクション**: Commands ページが更新された。「問題が起きたとき」の節で `/doctor` が「診断し修正もできるセットアップ点検」と説明し直され、全コマンド表では `/doctor` がバンドルスキルとして刷新（旧版の診断画面から `f` で送信する挙動は廃止）されたほか、`/effort` の非対話モード対応やモデル既定 effort ホールド時の `Not applied` などが反映された。 — [commands](https://code.claude.com/docs/en/commands#commands-across-a-typical-workflow) / [commands](https://code.claude.com/docs/en/commands#all-commands)

### ⌨️ コマンド・ワークフロー — 13件

- **Claude Code changelog**: changelog に v2.1.206（2026年7月9日）が追加された。`/cd` のパス補完、`CLAUDE.md` の削減を提案する `/doctor` チェック、`/commit-push-pr` の push リモート自動許可、`--mcp-config` 等での per-server `request_timeout_ms` 無視の修正、OAuth MCP サーバの再認証不具合の修正、`/model` ピッカーの価格表示不具合の修正、Bedrock 起動ハングの修正など多数の追加・修正が記載された。 — [English](https://code.claude.com/docs/en/changelog)
- **Use Claude Code on the web › Work with sessions**: Claude Code on the web のクラウドセッションで、`/model`・`/effort`・`/fast`・`/color`・`/rename` が引数付き（例 `/model sonnet`）で使えるようになり、`/config` も `key=value` で設定できるようになった。従来は対話ピッカーを開くコマンドは利用不可とされていた。引数形式には v2.1.205 以降が必要。 — [English](https://code.claude.com/docs/en/claude-code-on-the-web#work-with-sessions)
- **Desktop application › Coming from the CLI?**: デスクトップの「CLI からの移行」比較表が更新された。権限モード表記が Manual/Accept edits/Plan に改称され、サードパーティプロバイダの案内で研究プレビュー名「Cowork on 3P」が「Claude Desktop on 3P」に変更された。ターミナルダイアログ型コマンド（`/permissions`・`/config` 等）は Code タブで利用不可だが、`/config key=value` は使える点も明記された。 — [English](https://code.claude.com/docs/en/desktop#coming-from-the-cli)
- **Error reference › Command-line errors**: エラーリファレンスのコマンドラインエラー節が拡充された。`--json-schema` が JSON Schema として無効なら終了コード1で失敗し（`format` キーワードは注釈として受理）、`claude mcp add-from-claude-desktop` は名前が無効なサーバを報告しつつ他のサーバは取り込む。v2.1.205 より前は無効スキーマが無言で無視され、最初の失敗でインポート全体が止まっていた。 — [English](https://code.claude.com/docs/en/errors#command-line-errors)
- **Error reference › Report an error**: エラー報告の案内が更新され、シェルから `claude doctor` を実行して読み取り専用の診断を得るか、Claude Code 内で `/doctor` 点検を実行してセットアップ問題を発見・修正する方法に置き換えられた。 — [English](https://code.claude.com/docs/en/errors#report-an-error)
- **Run Claude Code programmatically › Examples**: headless（プログラム実行）の説明が更新された。無効な JSON Schema は `claude` がエラー終了し（`format` は注釈扱い）、`system/init` イベントに実装済みプロトコル挙動を示す `capabilities` 配列（例 `interrupt_receipt_v1`）が追加された。`-p` モードで `/model`・`/effort`・`/fast`・`/color`・`/rename` が引数付きで、`/mcp` が状態のテキスト要約として使える（v2.1.205 以降）。 — [English](https://code.claude.com/docs/en/headless#examples)
- **Continue local sessions from any device with Remote Control › Limitations**: Remote Control の制限が更新された。`/plugin` や `/resume` など端末専用コマンドはローカル CLI のみで動作し、`/model`・`/effort`・`/fast`・`/color`・`/rename` は引数付き（例 `/model sonnet`）でモバイル・Web から使えることが明記された。 — [English](https://code.claude.com/docs/en/remote-control#limitations)
- **Run parallel sessions with worktrees › Clean up worktrees**: worktrees の削除で、Windows では削除前にワークツリー内の任意の深さの NTFS ジャンクションやディレクトリシンボリックリンクをリンクエントリとして除去するようになった。v2.1.205 より前はトップレベルのリンクのみ扱い、サブディレクトリにネストしたジャンクションがあるとリンク先の外部ディレクトリの中身を削除しうる不具合があった。 — [English](https://code.claude.com/docs/en/worktrees#clean-up-worktrees)
- **CLI reference › CLI commands**: CLI リファレンスに `claude doctor` コマンドが追加された。セッションを開始せず、インストール健全性・設定ファイルの検証エラー・Remote Control 適格性などの読み取り専用診断を端末に出力する（修正も行うセッション内点検は `/doctor`）。 — [English](https://code.claude.com/docs/en/cli-reference#cli-commands)
- **CLI reference › CLI flags**: CLI フラグが更新された。`--append-subagent-system-prompt`（`-p` 時のみ、全サブエージェントのシステムプロンプト末尾へ追記、v2.1.205 以降）が追加され、`--json-schema` は無効スキーマでエラー終了し `format` を注釈として受理、`--max-turns` は `stream-json` 入力時に上限で終わったターンの後続メッセージをキューに残して独自の上限で実行することが明記された。 — [English](https://code.claude.com/docs/en/cli-reference#cli-flags)
- **1ページ・2セクション**: Commands ページが更新された。「問題が起きたとき」の節で `/doctor` が「診断し修正もできるセットアップ点検」と説明し直され、全コマンド表では `/doctor` がバンドルスキルとして刷新（旧版の診断画面から `f` で送信する挙動は廃止）されたほか、`/effort` の非対話モード対応やモデル既定 effort ホールド時の `Not applied` などが反映された。 — [commands](https://code.claude.com/docs/en/commands#commands-across-a-typical-workflow) / [commands](https://code.claude.com/docs/en/commands#all-commands)
- **Environment variables › Variables**: 環境変数が更新された。`CLAUDE_CODE_DISABLE_BUNDLED_SKILLS`（`/doctor` は例外で typable のまま）、サブエージェントのシステムプロンプト追記を有効化する `CLAUDE_CODE_ENABLE_APPEND_SUBAGENT_PROMPT`（v2.1.205 以降・フラグが自動設定）、`/doctor` スキルとその `/checkup` エイリアスを隠す `DISABLE_DOCTOR_COMMAND` の説明が更新された。 — [English](https://code.claude.com/docs/en/env-vars#variables)
- **1ページ・3セクション**: キーバインドの `Doctor` コンテキストと `doctor:fix` アクションが削除された（v2.1.205 で `/doctor` 診断画面が廃止されたため）。あわせてキーバインドの警告確認方法が `/doctor` からデバッグログ（`--debug`）へ変わった。 — [keybindings](https://code.claude.com/docs/en/keybindings#contexts) / [keybindings](https://code.claude.com/docs/en/keybindings#available-actions) / [keybindings](https://code.claude.com/docs/en/keybindings#validation)

### 今回変更のなかったカテゴリ

**❓ 未分類（語彙マッチなし・要 LLM 裁定）**

## 分類の内訳（付録）

LLM（Opus）による意味分類と、語彙マッチによる機械分類（参考）の対比。

| エントリ | LLM 分類 | 機械分類（参考） | 統合項目数 |
|---|---|---|---:|
| agent-view#Monitor sessions with agent view ほか | セッション・コンテキスト, モデル・利用状況 | Agent SDK・サブエージェント, セッション・コンテキスト | 2 |
| agent-view#How background sessions are hosted | Agent SDK・サブエージェント, セッション・コンテキスト | Agent SDK・サブエージェント | 1 |
| auto-mode-config#Define trusted infrastructure | セッション・コンテキスト | セッション・コンテキスト | 1 |
| changelog | コマンド・ワークフロー, MCP・外部連携, モデル・利用状況 | MCP・外部連携 | 1 |
| claude-code-on-the-web#Work with sessions | アプリ・プラットフォーム, コマンド・ワークフロー | アプリ・プラットフォーム | 1 |
| debug-your-config#See what loaded into context ほか | 設定・環境変数 | 設定・環境変数 | 2 |
| debug-your-config#Check hooks | 設定・環境変数, 拡張機能（プラグイン・スキル・フック） | 設定・環境変数 | 1 |
| desktop ほか | アプリ・プラットフォーム, セッション・コンテキスト | アプリ・プラットフォーム | 3 |
| desktop#Work with code | パーミッション・セキュリティ, アプリ・プラットフォーム | パーミッション・セキュリティ | 1 |
| desktop#Enterprise configuration | アプリ・プラットフォーム | アプリ・プラットフォーム | 1 |
| desktop#Coming from the CLI? | エンタープライズ基盤, アプリ・プラットフォーム, コマンド・ワークフロー | エンタープライズ基盤 | 1 |
| desktop-quickstart#Start your first session ほか | アプリ・プラットフォーム | アプリ・プラットフォーム | 2 |
| errors#Find your error | 拡張機能（プラグイン・スキル・フック）, MCP・外部連携 | 拡張機能（プラグイン・スキル・フック） | 1 |
| errors#Network and connection errors ほか | エンタープライズ基盤 | エンタープライズ基盤 | 2 |
| errors#Request errors | モデル・利用状況 | モデル・利用状況 | 1 |
| errors#Command-line errors | 拡張機能（プラグイン・スキル・フック）, MCP・外部連携, コマンド・ワークフロー | 拡張機能（プラグイン・スキル・フック） | 1 |
| errors#Responses seem lower quality than usual | MCP・外部連携, モデル・利用状況 | MCP・外部連携 | 1 |
| errors#Report an error | コマンド・ワークフロー | パーミッション・セキュリティ | 1 |
| fast-mode#Toggle fast mode ほか | モデル・利用状況, セッション・コンテキスト | モデル・利用状況 | 2 |
| feature-availability#Availability by model provider | アプリ・プラットフォーム, エンタープライズ基盤 | アプリ・プラットフォーム | 1 |
| headless#Examples | コマンド・ワークフロー, 拡張機能（プラグイン・スキル・フック） | コマンド・ワークフロー | 1 |
| how-claude-code-works#Stay safe with checkpoints and permissions ほか | セッション・コンテキスト, パーミッション・セキュリティ | セッション・コンテキスト | 2 |
| llm-gateway-connect#Configure each surface | エンタープライズ基盤, アプリ・プラットフォーム | エンタープライズ基盤 | 1 |
| llm-gateway-rollout#Rollout steps | エンタープライズ基盤, アプリ・プラットフォーム | エンタープライズ基盤 | 1 |
| mcp#Installing MCP servers | MCP・外部連携, アプリ・プラットフォーム | MCP・外部連携 | 1 |
| mcp#Import MCP servers from Claude Desktop | MCP・外部連携 | MCP・外部連携 | 1 |
| model-config#Available models | モデル・利用状況, 設定・環境変数 | モデル・利用状況 | 1 |
| model-config#Restrict model selection | モデル・利用状況 | モデル・利用状況 | 1 |
| model-config#Special model behavior | モデル・利用状況 | モデル・利用状況 | 1 |
| monitoring-usage#Configuration details | モデル・利用状況 | モデル・利用状況 | 1 |
| permission-modes#Switch permission modes | パーミッション・セキュリティ | パーミッション・セキュリティ | 1 |
| permission-modes#Analyze before you edit with plan mode | パーミッション・セキュリティ, セッション・コンテキスト | パーミッション・セキュリティ | 1 |
| permissions#Permission modes | パーミッション・セキュリティ | パーミッション・セキュリティ | 1 |
| platforms#Where to run Claude Code | アプリ・プラットフォーム | アプリ・プラットフォーム | 1 |
| plugin-dependencies#How constraints interact ほか | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 2 |
| plugin-marketplaces#Marketplace schema | 拡張機能（プラグイン・スキル・フック）, パーミッション・セキュリティ | 拡張機能（プラグイン・スキル・フック） | 1 |
| remote-control#Limitations | アプリ・プラットフォーム, コマンド・ワークフロー | アプリ・プラットフォーム | 1 |
| sandboxing#Configure sandboxing | パーミッション・セキュリティ, エンタープライズ基盤 | パーミッション・セキュリティ | 1 |
| settings#Settings files | 設定・環境変数, 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |
| skills#Bundled skills | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |
| skills#Troubleshooting | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |
| sub-agents#Configure subagents | Agent SDK・サブエージェント | Agent SDK・サブエージェント | 1 |
| sub-agents#Work with subagents | Agent SDK・サブエージェント | Agent SDK・サブエージェント | 1 |
| troubleshoot-install#Common installation issues | アプリ・プラットフォーム | アプリ・プラットフォーム | 1 |
| troubleshooting ほか | MCP・外部連携 | MCP・外部連携, 未分類（語彙マッチなし・要 LLM 裁定） | 3 |
| vs-code#Use the prompt box | アプリ・プラットフォーム | アプリ・プラットフォーム | 1 |
| web-quickstart#Compare ways to run Claude Code | アプリ・プラットフォーム | アプリ・プラットフォーム | 1 |
| web-quickstart#Start a task | パーミッション・セキュリティ, アプリ・プラットフォーム | パーミッション・セキュリティ | 1 |
| worktrees#Start Claude in a worktree | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |
| worktrees#Clean up worktrees | コマンド・ワークフロー | コマンド・ワークフロー | 1 |
| agent-sdk/agent-loop#Control how the loop runs | Agent SDK・サブエージェント | Agent SDK・サブエージェント | 1 |
| agent-sdk/structured-outputs#Output format configuration | Agent SDK・サブエージェント | Agent SDK・サブエージェント | 1 |
| agent-sdk/typescript#Types | Agent SDK・サブエージェント | Agent SDK・サブエージェント | 1 |
| agent-sdk/typescript#Message Types | Agent SDK・サブエージェント, MCP・外部連携 | Agent SDK・サブエージェント | 1 |
| agent-sdk/typescript#Tool Input Types | Agent SDK・サブエージェント | Agent SDK・サブエージェント | 1 |
| cli-reference#CLI commands | コマンド・ワークフロー | コマンド・ワークフロー | 1 |
| cli-reference#CLI flags | コマンド・ワークフロー, Agent SDK・サブエージェント | コマンド・ワークフロー | 1 |
| commands#Commands across a typical workflow ほか | コマンド・ワークフロー, セッション・コンテキスト | コマンド・ワークフロー, セッション・コンテキスト | 2 |
| env-vars#Variables | コマンド・ワークフロー, 拡張機能（プラグイン・スキル・フック）, 設定・環境変数 | コマンド・ワークフロー | 1 |
| hooks#Hook events | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |
| keybindings#Contexts ほか | コマンド・ワークフロー | コマンド・ワークフロー | 3 |
| plugins-reference#Plugin components reference | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |
| plugins-reference#Plugin manifest schema | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |
| statusline#Subagent status lines | 拡張機能（プラグイン・スキル・フック）, モデル・利用状況 | 拡張機能（プラグイン・スキル・フック） | 1 |

<!--
base_commit: c5b9ea9
head_commit: 191b578
generator: build_category_summary.py render --entries (trial v2)
-->
