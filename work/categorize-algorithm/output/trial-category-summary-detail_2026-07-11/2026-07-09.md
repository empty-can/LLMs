---
対象期間: 2026年07月08日 〜 2026年07月09日
作成日: 2026-07-09
形式: カテゴリ別詳細・試作版 v3（Opus 意味分類・カテゴリ内統合・複数カテゴリ掲載・内容ラベル見出し・エンタープライズサブ区分・ドキュメント改善分離）
---

# Claude Code 公式ドキュメント更新サマリ（カテゴリ別詳細・試作 v3）

```markdown
今回の変更 78 項目を 42 エントリに集約。カテゴリ別の掲載数は 「拡張機能（プラグイン・スキル・フック）」9件、「アプリ・プラットフォーム」6件、「パーミッション・セキュリティ」6件、「コマンド」5件、「サブエージェント」4件、「ワークフロー」4件、「Agent SDK」4件、「エンタープライズ基盤」3件、「MCP・外部連携」3件、「モデル・利用状況」3件、「設定・環境変数」3件、「セッション・コンテキスト」2件、「ドキュメント改善」1件（1 エントリが複数カテゴリに掲載される場合は重複計上）。
```

## カテゴリ別の変更点

### ⌨️ コマンド — 5件

- **/doctor のセットアップ点検化**: `/doctor` が読み取り専用の診断画面から、インストール状態・設定ファイル・未使用の拡張機能・コンテキスト消費を点検し、確認を取ったうえで修正まで適用する「セットアップ点検」へ刷新された。セッションを起動せずに読み取り専用の診断だけを出す `claude doctor` ターミナルコマンドが新設され、各ページの案内もそちらへ振り分けられた。これに伴い `Doctor` キーバインドコンテキストと `doctor:fix` アクションは廃止され、キーバインドの警告やサンドボックスの設定不備はデバッグログや起動時メッセージで確認する形になった。 — [debug-your-config](https://code.claude.com/docs/en/debug-your-config#see-what-loaded-into-context) / [debug-your-config](https://code.claude.com/docs/en/debug-your-config#check-resolved-settings) / [errors](https://code.claude.com/docs/en/errors#network-and-connection-errors) ほか13箇所
- **非対話・リモートでの引数形式コマンド**: `/model`・`/effort`・`/fast`・`/color`・`/rename` が値を引数に取る形式（例 `/model sonnet`）で、非対話モード（`-p`）、Claude Code on the web のクラウドセッション、Remote Control のモバイル・Web から使えるようになった（v2.1.205 以降）。`/config` は `key=value` を渡せば設定を変更でき、`/plugin` や `/resume` などターミナル UI でしか動かないコマンドは引き続きローカル CLI 限定である。 — [claude-code-on-the-web](https://code.claude.com/docs/en/claude-code-on-the-web#work-with-sessions) / [remote-control](https://code.claude.com/docs/en/remote-control#limitations) / [commands](https://code.claude.com/docs/en/commands#all-commands)
- **CLI フラグの追加と挙動変更**: `--append-subagent-system-prompt` が追加され、非対話モードで入れ子を含む全サブエージェントのシステムプロンプト末尾に任意のテキストを追記できるようになった（v2.1.205 以降）。`--json-schema` は不正なスキーマでエラー終了して `format` キーワードを注釈として受け付けるようになり、`--max-turns` は上限到達時に届いたメッセージを破棄せず、独自の上限を持つ次のターンとして実行するようになった。 — [English](https://code.claude.com/docs/en/cli-reference#cli-flags)
- **--json-schema の検証エラー新設**: `--json-schema` に渡した値が JSON Schema としてコンパイルできない場合、`claude` は検証器の診断メッセージを添えて終了コード 1 で終わるようになった（v2.1.205 より前は不正スキーマが黙って無視され非構造テキストが返っていた）。パース不能な値や JSON オブジェクトでない値には専用のエラーが出て、`format` キーワードは注釈として受理される。 — [English](https://code.claude.com/docs/en/errors#command-line-errors)
- **v2.1.206 更新ログ追加**: `/cd` に `/add-dir` と同じディレクトリパス補完が追加され、`/doctor` にはコードベースから導出できる内容を削ってリポジトリ管理下の `CLAUDE.md` を圧縮する提案チェックが加わった。`claude --resume` / `--continue` が起動時にキー入力を受け付けない不具合、`/status` が同じインストール警告を二重に出す不具合、フルスクリーンの最下部ジャンプ表示の不具合が修正された。`claude-opus-4-8` での `/code-review` の指摘品質も改善された。 — [English](https://code.claude.com/docs/en/changelog)

### 🧰 拡張機能（プラグイン・スキル・フック） — 9件

- **hook matcher の失敗パターン**: hook が登録されているのに発火しない場合の原因が箇条書きに整理された。`matcher` に配列を指定するのはスキーマエラーで、ユーザー・プロジェクト・ローカルの設定ファイルはファイルごと拒否され、そのファイルの hook は `/hooks` に一切現れなくなる。一方、管理設定では不正なエントリだけが取り除かれ、同じファイルの他の hook は有効なまま残る。 — [English](https://code.claude.com/docs/en/debug-your-config#check-hooks)
- **プラグイン診断の表示先変更**: プラグインの依存関係エラーや、マニフェスト指定によって無視された既定フォルダの警告は `/doctor` に出なくなり、`claude plugin list` と `/plugin` インターフェース（Errors タブ・詳細ビュー）で確認する形になった。依存エラーのあるプラグインが解決まで無効化される挙動は従来どおり。 — [plugin-dependencies](https://code.claude.com/docs/en/plugin-dependencies#how-constraints-interact) / [plugin-dependencies](https://code.claude.com/docs/en/plugin-dependencies#resolve-dependency-errors) / [plugins-reference](https://code.claude.com/docs/en/plugins-reference#plugin-manifest-schema)
- **予約マーケットプレイス名の再検査**: Anthropic 公式用に予約されたマーケットプレイス名に `first-party-plugins` と `healthcare` が追加された。さらに予約名の検査が追加時だけでなく読み込みのたびに行われるようになり、予約される前に登録済みだったマーケットプレイスも「信頼できないソースから登録されている」と報告されて読み込まれなくなる。サードパーティのマーケットプレイスは別名で再登録すれば再び読み込まれる。 — [English](https://code.claude.com/docs/en/plugin-marketplaces#marketplace-schema)
- **バンドルスキル化した /doctor**: `/doctor` がバンドルスキルになり、`disableBundledSkills` や `CLAUDE_CODE_DISABLE_BUNDLED_SKILLS` を有効にしても入力可能なまま残る唯一の例外となった。非表示にするには `DISABLE_DOCTOR_COMMAND` を `1` にするか、`skillOverrides` に `"doctor": "off"` を設定する。設定表には管理設定限定の `browserExternalPageTools` が、環境変数にはサブエージェントへのプロンプト追記を有効化する `CLAUDE_CODE_ENABLE_APPEND_SUBAGENT_PROMPT` が追加された。 — [settings](https://code.claude.com/docs/en/settings#settings-files) / [skills](https://code.claude.com/docs/en/skills#bundled-skills) / [env-vars](https://code.claude.com/docs/en/env-vars#variables)
- **スキル一覧のコンテキスト予算**: スキル一覧が予算（既定でコンテキストウィンドウの 1%）を超えると、起動回数の少ないスキルから説明文が落とされて名前だけが残る仕組みが整理して記載された。一覧のコンテキスト消費とその主因は `/doctor` が見積もり、予算超過時は `--debug` で見られるデバッグログにも警告が書き出される。 — [English](https://code.claude.com/docs/en/skills#troubleshooting)
- **LSP サーバーの設定と拡張子競合**: プラグインの LSP サーバー設定で `shutdownTimeout` と `restartOnCrash` が実際に使えるようになった（v2.1.205 以降。従来はスキーマ上は受理されるものの、指定するとそのサーバーが起動時に丸ごと無視されていた）。同じ拡張子を宣言する LSP サーバーが複数ある場合は最初に登録されたものが担当し、`/plugin` が有効なプラグイン名を示す警告を出す。初期化に失敗したサーバーは拡張子を占有しなくなり、同じ拡張子を宣言する別の正常なサーバーが処理を引き継げるようになった。 — [English](https://code.claude.com/docs/en/plugins-reference#plugin-components-reference)
- **サブエージェント行のモデル情報**: サブエージェント用ステータスラインへ渡される各タスクの情報に、解決済みのモデル ID を示す `model` と、そのモデルのコンテキストウィンドウサイズ `contextWindowSize` が追加された（v2.1.205 以降）。`tokenCount` と組み合わせて行ごとの使用率を描画できる。モデルが未解決のタスクでは両フィールドとも省略される。 — [English](https://code.claude.com/docs/en/statusline#subagent-status-lines)
- **WorktreeCreate hook のパス処理**: `WorktreeCreate` hook は作成したワークツリーのパスを stdout の最後の非空行に出力すればよくなり、ANSI エスケープは除去され、相対パスは hook を実行したディレクトリを基準に解決される。パスがディレクトリとして開けない場合はセッションがクラッシュせず、パスを示すエラーを出して終了コード 1 で終わるようになった（v2.1.205 より前は `-p` で約 30 秒停止した末に終了コード 0 で終わっていた）。あわせて `ExitPlanMode` の `allowedPrompts` フィールドは非推奨となり、受理はされるが無視される。 — [worktrees](https://code.claude.com/docs/en/worktrees#start-claude-in-a-worktree) / [hooks](https://code.claude.com/docs/en/hooks#hook-events)
- **v2.1.206 更新ログ追加**: LSP プラグインに対して誤った「未使用プラグイン」の案内が表示され、未使用判定のテレメトリも偏る不具合が修正された。 — [English](https://code.claude.com/docs/en/changelog)

### 🔐 パーミッション・セキュリティ — 6件

- **auto mode の可視性判定の根拠**: auto mode の分類器がリポジトリを公開とみなす根拠は、分類器自身が読める会話内容（ユーザーの発言や Claude が実行したコマンド）に限られると明記された。コマンドの出力は分類器に渡らないため、`gh repo view` を実行しただけでは公開の根拠にならず、リポジトリは非公開として扱われる。 — [English](https://code.claude.com/docs/en/auto-mode-config#define-trusted-infrastructure)
- **auto mode の既定ブロック追加**: v2.1.205 以降、auto mode は `~/.claude/projects/` 配下のセッショントランスクリプト（`.jsonl`）への書き込みと、会話中に代入が見当たらないシェル変数を対象にした再帰強制削除（`rm -rf "$VAR"` 等）を既定でブロックするようになった。削除は実パスを明示すれば通り、トランスクリプトの読み取りは制限されない。 — [English](https://code.claude.com/docs/en/permission-modes#analyze-before-you-edit-with-plan-mode)
- **パーミッションモードの名称統一**: パーミッションモードの表示名が全面的に変更された。設定キー `default` は「Manual」、`acceptEdits` は「Accept edits」、`plan` は「Plan」、`auto` は「Auto」と表記され、従来の「Ask permissions」「Auto accept edits」「Plan mode」「Auto mode」という呼称は使われなくなった（VS Code 拡張は v2.1.205 で追随）。設定キーそのものは変わらない。 — [desktop-quickstart](https://code.claude.com/docs/en/desktop-quickstart#start-your-first-session) / [how-claude-code-works](https://code.claude.com/docs/en/how-claude-code-works#stay-safe-with-checkpoints-and-permissions) / [permission-modes](https://code.claude.com/docs/en/permission-modes#switch-permission-modes) ほか4箇所
- **外部サイト操作の安全性チェック**: 新規セクションとして、デスクトップの Browser ペインでドキュメントや課題管理ツールなどの外部サイトを開き、Claude がそれらを読み取り・操作できることが記載された。外部ページへのクリックや入力といった書き込み操作は全パーミッションモードで安全性分類器の審査を受け、フラグが立つとモードに関係なく許可プロンプトが出る。Auto と Bypass permissions 以外のモードでは新しいサイトへ遷移する前にドメイン許可リストの確認も入り、管理設定 `browserExternalPageTools` を `"disabled"` にすれば外部ページ操作ツール自体を無効化できる。 — [desktop](https://code.claude.com/docs/en/desktop#work-with-code) / [desktop](https://code.claude.com/docs/en/desktop#enterprise-configuration)
- **予約マーケットプレイス名の再検査**: Anthropic 公式用に予約されたマーケットプレイス名に `first-party-plugins` と `healthcare` が追加された。さらに予約名の検査が追加時だけでなく読み込みのたびに行われるようになり、予約される前に登録済みだったマーケットプレイスも「信頼できないソースから登録されている」と報告されて読み込まれなくなる。サードパーティのマーケットプレイスは別名で再登録すれば再び読み込まれる。 — [English](https://code.claude.com/docs/en/plugin-marketplaces#marketplace-schema)
- **v2.1.206 更新ログ追加**: `/commit-push-pr` が `origin` に加えて、リポジトリに設定された push 先リモート（`remote.pushDefault`、またはリモートが 1 つだけならそれ）への `git push` を自動許可するようになった。`EnterWorktree` はプロジェクトの `.claude/worktrees/` 外にある git ワークツリーへ入る前に確認を求めるようになった。 — [English](https://code.claude.com/docs/en/changelog)

### 📚 セッション・コンテキスト — 2件

- **エージェントビューの行表示刷新**: エージェントビューの行サマリが、生のツール呼び出しや `done/total` 表示ではなくセッション自身の一行報告（64 桁で切り詰め）を示すようになり、ディレクトリ別グループの行には色付きの状態語が付くようになった。ピークパネルは完全な状態文と入力待ちセッションの質問を先頭に表示するようになり、`gh` での編集・コメント・クローズ・ready 化でも PR がセッションに紐づくほか、可読文字を含まないターンは直前の状態を保持して入力待ちが `Working` に戻らなくなった。`claude attach` は再起動中のセッションを、待機理由を示すステータス行付きで最大約 60 秒待つようになった。 — [agent-view](https://code.claude.com/docs/en/agent-view#monitor-sessions-with-agent-view) / [agent-view](https://code.claude.com/docs/en/agent-view#how-background-sessions-are-hosted) / [agent-view](https://code.claude.com/docs/en/agent-view#version-history)
- **auto mode の既定ブロック追加**: v2.1.205 以降、auto mode は `~/.claude/projects/` 配下のセッショントランスクリプト（`.jsonl`）への書き込みと、会話中に代入が見当たらないシェル変数を対象にした再帰強制削除（`rm -rf "$VAR"` 等）を既定でブロックするようになった。削除は実パスを明示すれば通り、トランスクリプトの読み取りは制限されない。 — [English](https://code.claude.com/docs/en/permission-modes#analyze-before-you-edit-with-plan-mode)

### 🤝 サブエージェント — 4件

- **エージェントビューの行表示刷新**: エージェントビューの行サマリが、生のツール呼び出しや `done/total` 表示ではなくセッション自身の一行報告（64 桁で切り詰め）を示すようになり、ディレクトリ別グループの行には色付きの状態語が付くようになった。ピークパネルは完全な状態文と入力待ちセッションの質問を先頭に表示するようになり、`gh` での編集・コメント・クローズ・ready 化でも PR がセッションに紐づくほか、可読文字を含まないターンは直前の状態を保持して入力待ちが `Working` に戻らなくなった。`claude attach` は再起動中のセッションを、待機理由を示すステータス行付きで最大約 60 秒待つようになった。 — [agent-view](https://code.claude.com/docs/en/agent-view#monitor-sessions-with-agent-view) / [agent-view](https://code.claude.com/docs/en/agent-view#how-background-sessions-are-hosted) / [agent-view](https://code.claude.com/docs/en/agent-view#version-history)
- **サブエージェント行のモデル情報**: サブエージェント用ステータスラインへ渡される各タスクの情報に、解決済みのモデル ID を示す `model` と、そのモデルのコンテキストウィンドウサイズ `contextWindowSize` が追加された（v2.1.205 以降）。`tokenCount` と組み合わせて行ごとの使用率を描画できる。モデルが未解決のタスクでは両フィールドとも省略される。 — [English](https://code.claude.com/docs/en/statusline#subagent-status-lines)
- **サブエージェントの重複名と再開**: 同一ディレクトリ配下で名前が重複するサブエージェント定義を `/doctor` の点検が検出し、1 つを残す改名や削除を提案するようになった。非対話モードでは `--append-subagent-system-prompt` により、入れ子を含む全サブエージェントのシステムプロンプト末尾へテキストを追記できる（v2.1.205 以降）。サブエージェントを再開すると同じ ID で新しい実行が始まり、タスク一覧や Agent SDK のタスクイベントでも「実行中」と表示されるようになった（従来は以前の失敗・完了状態を表示し続けていた）。 — [sub-agents](https://code.claude.com/docs/en/sub-agents#configure-subagents) / [sub-agents](https://code.claude.com/docs/en/sub-agents#work-with-subagents)
- **v2.1.206 更新ログ追加**: バックグラウンドエージェントは Claude Code の更新直後にバックグラウンドで新バージョンへ切り替わるようになり、アタッチ時に遅い更新処理を待たされなくなった。エージェントビューではステータス列がターミナル幅いっぱいを使うようになり、Ctrl+X で完了したセッションを恒久的に削除できるようになった（セッションの二重描画や削除済みジョブの復活も解消）。`claude rm` したジョブが daemon のロスターに残って再表示される不具合も修正された。 — [English](https://code.claude.com/docs/en/changelog)

### 🔁 ワークフロー — 4件

- **構造化出力の検証と capabilities**: 不正な JSON Schema は起動時にエラーで失敗するようになり、`format` キーワードは注釈として受理されるようになった（v2.1.205 より前は不正スキーマが黙って無視されて非構造テキストが返り、`format` を含むスキーマは一律に不正扱いだった）。`system/init` イベントには、CLI が実装するプロトコル挙動を列挙する `capabilities` 配列（例 `interrupt_receipt_v1`）が追加され、バージョン文字列の比較ではなく機能検出で分岐できるようになった。 — [headless](https://code.claude.com/docs/en/headless#examples) / [agent-sdk/structured-outputs](https://code.claude.com/docs/en/agent-sdk/structured-outputs#output-format-configuration)
- **WorktreeCreate hook のパス処理**: `WorktreeCreate` hook は作成したワークツリーのパスを stdout の最後の非空行に出力すればよくなり、ANSI エスケープは除去され、相対パスは hook を実行したディレクトリを基準に解決される。パスがディレクトリとして開けない場合はセッションがクラッシュせず、パスを示すエラーを出して終了コード 1 で終わるようになった（v2.1.205 より前は `-p` で約 30 秒停止した末に終了コード 0 で終わっていた）。あわせて `ExitPlanMode` の `allowedPrompts` フィールドは非推奨となり、受理はされるが無視される。 — [worktrees](https://code.claude.com/docs/en/worktrees#start-claude-in-a-worktree) / [hooks](https://code.claude.com/docs/en/hooks#hook-events)
- **Windows のワークツリー削除**: Windows でワークツリーを削除する際、内部の任意の階層にある NTFS ジャンクションやディレクトリシンボリックリンクをリンクとして先に取り除くようになり、リンク先の実ファイルまで消える事故を防ぐようになった。v2.1.205 より前はトップレベルのリンクしか扱われず、サブディレクトリ内のジャンクション経由でワークツリー外のディレクトリの中身が削除されることがあった。 — [English](https://code.claude.com/docs/en/worktrees#clean-up-worktrees)
- **v2.1.206 更新ログ追加**: ワークフロー詳細ビューで、左矢印キーがフェーズやエージェントから 1 つ上の階層へ戻れない不具合が修正された。 — [English](https://code.claude.com/docs/en/changelog)

### ⚙️ 設定・環境変数 — 3件

- **hook matcher の失敗パターン**: hook が登録されているのに発火しない場合の原因が箇条書きに整理された。`matcher` に配列を指定するのはスキーマエラーで、ユーザー・プロジェクト・ローカルの設定ファイルはファイルごと拒否され、そのファイルの hook は `/hooks` に一切現れなくなる。一方、管理設定では不正なエントリだけが取り除かれ、同じファイルの他の hook は有効なまま残る。 — [English](https://code.claude.com/docs/en/debug-your-config#check-hooks)
- **バンドルスキル化した /doctor**: `/doctor` がバンドルスキルになり、`disableBundledSkills` や `CLAUDE_CODE_DISABLE_BUNDLED_SKILLS` を有効にしても入力可能なまま残る唯一の例外となった。非表示にするには `DISABLE_DOCTOR_COMMAND` を `1` にするか、`skillOverrides` に `"doctor": "off"` を設定する。設定表には管理設定限定の `browserExternalPageTools` が、環境変数にはサブエージェントへのプロンプト追記を有効化する `CLAUDE_CODE_ENABLE_APPEND_SUBAGENT_PROMPT` が追加された。 — [settings](https://code.claude.com/docs/en/settings#settings-files) / [skills](https://code.claude.com/docs/en/skills#bundled-skills) / [env-vars](https://code.claude.com/docs/en/env-vars#variables)
- **v2.1.206 更新ログ追加**: `CLAUDE_CODE_EXTRA_BODY` が `claude agents` や `--bg` のバックグラウンドワーカーで黙って無視される不具合が修正され、シェルでエクスポートした上書き値が起動元セッションに追従するようになった。 — [English](https://code.claude.com/docs/en/changelog)

### 🖥 アプリ・プラットフォーム — 6件

- **非対話・リモートでの引数形式コマンド**: `/model`・`/effort`・`/fast`・`/color`・`/rename` が値を引数に取る形式（例 `/model sonnet`）で、非対話モード（`-p`）、Claude Code on the web のクラウドセッション、Remote Control のモバイル・Web から使えるようになった（v2.1.205 以降）。`/config` は `key=value` を渡せば設定を変更でき、`/plugin` や `/resume` などターミナル UI でしか動かないコマンドは引き続きローカル CLI 限定である。 — [claude-code-on-the-web](https://code.claude.com/docs/en/claude-code-on-the-web#work-with-sessions) / [remote-control](https://code.claude.com/docs/en/remote-control#limitations) / [commands](https://code.claude.com/docs/en/commands#all-commands)
- **プレビューペインの Browser ペイン化**: デスクトップアプリのプレビューペインが、タブ型ブラウザである「Browser ペイン」へ改称・拡張され、実行中のアプリに加えて外部サイトも並べて開けるようになった。開発サーバーの起動・停止や `launch.json` の編集は Preview ドロップダウンではなくサーバードロップダウンから行う。ショートカットも整理され、Browser ペインの開閉（Cmd/Ctrl+Shift+B）に加えて、サイドチャット・表示モード切替・パーミッションモード／モデル／effort の各メニューを開く割り当てが追加された。 — [desktop](https://code.claude.com/docs/en/desktop) / [desktop](https://code.claude.com/docs/en/desktop#arrange-your-workspace) / [desktop](https://code.claude.com/docs/en/desktop#extend-claude-code) ほか1箇所
- **外部サイト操作の安全性チェック**: 新規セクションとして、デスクトップの Browser ペインでドキュメントや課題管理ツールなどの外部サイトを開き、Claude がそれらを読み取り・操作できることが記載された。外部ページへのクリックや入力といった書き込み操作は全パーミッションモードで安全性分類器の審査を受け、フラグが立つとモードに関係なく許可プロンプトが出る。Auto と Bypass permissions 以外のモードでは新しいサイトへ遷移する前にドメイン許可リストの確認も入り、管理設定 `browserExternalPageTools` を `"disabled"` にすれば外部ページ操作ツール自体を無効化できる。 — [desktop](https://code.claude.com/docs/en/desktop#work-with-code) / [desktop](https://code.claude.com/docs/en/desktop#enterprise-configuration)
- **「Claude Desktop on 3P」への改称**: サードパーティプロバイダ上でデスクトップの Code タブを動かす仕組みの名称が「Cowork on 3P research preview」から「Claude Desktop on 3P」へ変わり、参照先ドキュメントも `claude.com/docs/third-party/claude-desktop/` 配下へ移った。Amazon Bedrock・Google Cloud's Agent Platform・Microsoft Foundry・自前の LLM ゲートウェイ上で Code タブを動かせる点と、Enterprise の管理設定で Google Cloud's Agent Platform やゲートウェイへ振り向けられる点は従来どおり。 — [desktop](https://code.claude.com/docs/en/desktop#coming-from-the-cli) / [feature-availability](https://code.claude.com/docs/en/feature-availability#availability-by-model-provider) / [llm-gateway-connect](https://code.claude.com/docs/en/llm-gateway-connect#configure-each-surface) ほか2箇所
- **インストール時のエラー表示**: 接続は維持されたまま 10 分の期限内に終わらないダウンロードは `Download timed out: exceeded the total deadline` として失敗し、自動では再試行されないことが明記された（v2.1.205 より前は HTTP クライアントの汎用タイムアウトとして表示されていた）。FreeBSD ではインストーラが非対応プラットフォームとして報告するようになり、以前のように Linux 用バイナリを取得して動かない状態に陥らなくなった。 — [errors](https://code.claude.com/docs/en/errors#installation-errors) / [troubleshoot-install](https://code.claude.com/docs/en/troubleshoot-install#common-installation-issues)
- **v2.1.206 更新ログ追加**: デスクトップセッションがターン途中でスラッシュコマンドを送ると「running」表示のまま固まる不具合と、Windows で素の `claude --resume` の前にセットアッププロンプトが出るとエージェントビューのキー入力が無視される不具合が修正された。ログアウト状態での `/remote-control` は「Unknown command」ではなくサインイン方法を案内するようになり、`/doctor` の更新チェックは Homebrew インストールを設定のチャンネルではなく cask のチャンネルと比較するようになった。 — [English](https://code.claude.com/docs/en/changelog)

### 🤖 モデル・利用状況 — 3件

- **モデル制限とエイリアス解決**: `availableModels` が特定バージョンを指定している場合でも、`opus`・`sonnet`・`haiku`・`fable` のファミリーエイリアスは許可された最新バージョンへ解決され、要求モデルと置換先を示す通知が出るようになった（Anthropic API と Claude Platform on AWS。全バージョンが禁止のときだけ拒否される）。plan mode の自動アップグレード（`opusplan` など）も許可された最新版を使うようになり、独自のデプロイ ID を使う Amazon Bedrock・Google Cloud's Agent Platform・Microsoft Foundry・Mantle ではセッションのモデルに留まる。非対話モードで `/model` や `/effort` を実行した場合は当該セッション限りの適用となり既定として保存されず、Fable 5・Opus 4.8・Opus 4.7 では既定 effort のホールド中に `/effort` が `Not applied` と報告される。 — [errors](https://code.claude.com/docs/en/errors#request-errors) / [model-config](https://code.claude.com/docs/en/model-config#available-models) / [model-config](https://code.claude.com/docs/en/model-config#restrict-model-selection) ほか1箇所
- **非対話モードでの fast mode**: 非対話モード（`-p`）では、`--settings '{"fastMode": true}'` を付けて起動したセッションでのみ `/fast` が使え、その切り替えも当該セッション限りで既定には保存されないことが明記された。それ以外の非対話セッションでは fast mode が使えない旨が報告される。対話セッションで有効にした fast mode が以後のセッションへ引き継がれる既定挙動は変わらない。 — [fast-mode](https://code.claude.com/docs/en/fast-mode#toggle-fast-mode) / [fast-mode](https://code.claude.com/docs/en/fast-mode#requirements)
- **v2.1.206 更新ログ追加**: ログインの期限切れが「選択したモデルに問題があります」という誤解を招くエラーになって全モデルが失敗する不具合が修正され、`/login` の実行を促すようになった。`/model` ピッカーで行に別モデルの価格が表示される不具合、課金しないプロバイダで一次価格を提示する挙動、権限や許可リストで行が落ちたときにサーバー提供モデルの行位置がずれる不具合も修正された。 — [English](https://code.claude.com/docs/en/changelog)

### 🔌 MCP・外部連携 — 3件

- **Claude Desktop からのインポート継続**: `claude mcp add-from-claude-desktop` は、名前に空白などの使えない文字を含むサーバーを 1 件ずつ理由付きで報告してスキップし、選択した残りのサーバーのインポートを続行するようになった（v2.1.205 より前は最初の失敗で中断し、1 件も追加されなかった）。`claude mcp` 系コマンドで使えるサーバー名は英数字・ハイフン・アンダースコアに限られる。 — [errors](https://code.claude.com/docs/en/errors#command-line-errors) / [mcp](https://code.claude.com/docs/en/mcp#import-mcp-servers-from-claude-desktop)
- **予約サーバー名と接続失敗の伝達**: `workspace` に加えて `claude-in-chrome`・`computer-use`・`Claude Preview`・`Claude Browser` が組み込みサーバー用の予約名になり、これらの名前を使った定義は読み込み時にスキップされ、`claude mcp add` はエラーで拒否するようになった。設定済みサーバーが接続に失敗した場合は、どのサーバーがどのエラーで失敗したかが Claude に伝わり、応答の中で接続失敗が報告されるようになった（tool search が有効な構成に限る）。 — [English](https://code.claude.com/docs/en/mcp#installing-mcp-servers)
- **v2.1.206 更新ログ追加**: `--mcp-config` や `.mcp.json` で設定したサーバーごとの `request_timeout_ms` が無視され、新規セッションで長時間の MCP ツール呼び出しが 60 秒の既定値でタイムアウトする不具合が修正された。OAuth の MCP サーバーがトークン更新に 1 度失敗しただけで手動の再認証を要求する不具合と、`--permission-prompt-tool` が MCP サーバーを指しているとコールドスタート時に「MCP tool not found」でクラッシュする不具合も修正された。 — [English](https://code.claude.com/docs/en/changelog)

### 🧩 Agent SDK — 4件

- **構造化出力の検証と capabilities**: 不正な JSON Schema は起動時にエラーで失敗するようになり、`format` キーワードは注釈として受理されるようになった（v2.1.205 より前は不正スキーマが黙って無視されて非構造テキストが返り、`format` を含むスキーマは一律に不正扱いだった）。`system/init` イベントには、CLI が実装するプロトコル挙動を列挙する `capabilities` 配列（例 `interrupt_receipt_v1`）が追加され、バージョン文字列の比較ではなく機能検出で分岐できるようになった。 — [headless](https://code.claude.com/docs/en/headless#examples) / [agent-sdk/structured-outputs](https://code.claude.com/docs/en/agent-sdk/structured-outputs#output-format-configuration)
- **max-turns 到達時のキュー保持**: ストリーミング入力では、ターン実行中に送ったメッセージが max-turns の上限でターンが終わってもキューに残り、独自の max-turns 上限を持つ新しいターンとして実行されるようになった。v2.1.205 より前は、ターンの最終イテレーションに届いたメッセージが終了するターンに取り込まれ、モデルへ届かないまま失われることがあった。 — [English](https://code.claude.com/docs/en/agent-sdk/agent-loop#control-how-the-loop-runs)
- **interrupt レシートと capabilities**: `interrupt()` が、割り込み後も残るキュー済みメッセージの UUID を列挙する `SDKControlInterruptResponse` を返すようになった（CLI が `interrupt_receipt_v1` を通知する場合。それ以前の CLI では `undefined`）。`SDKSystemMessage` には `capabilities` 配列が加わり、バージョン文字列の比較ではなく機能検出ができるようになった。peer 種別のメッセージには送信者の表示名 `name`（制御文字を除去し 64 コードポイントで切り詰め）と、封筒を取り除いた本文 `body` が付き、メッセージ本文を再パースせずに描画できる。 — [agent-sdk/typescript](https://code.claude.com/docs/en/agent-sdk/typescript#types) / [agent-sdk/typescript](https://code.claude.com/docs/en/agent-sdk/typescript#message-types)
- **ExitPlanMode の allowedPrompts 廃止**: `ExitPlanMode` の `allowedPrompts` フィールドが非推奨となり、既存の呼び出し元やトランスクリプトが検証を通るよう受理はされるものの無視されるようになった。v2.1.205 より前は、プランを実装するための Bash のプロンプトベース権限を要求するために使われていた。 — [English](https://code.claude.com/docs/en/agent-sdk/typescript#tool-input-types)

### 🏢 エンタープライズ基盤 — 3件

**ベンダー非依存**

- **v2.1.206 更新ログ追加**: `/login` が Anthropic 運営の公開ゲートウェイエンドポイントに対応した。 — [English](https://code.claude.com/docs/en/changelog)

**サードパーティのみ（Anthropic 以外の共通話題）**

- **「Claude Desktop on 3P」への改称**: サードパーティプロバイダ上でデスクトップの Code タブを動かす仕組みの名称が「Cowork on 3P research preview」から「Claude Desktop on 3P」へ変わり、参照先ドキュメントも `claude.com/docs/third-party/claude-desktop/` 配下へ移った。Amazon Bedrock・Google Cloud's Agent Platform・Microsoft Foundry・自前の LLM ゲートウェイ上で Code タブを動かせる点と、Enterprise の管理設定で Google Cloud's Agent Platform やゲートウェイへ振り向けられる点は従来どおり。 — [desktop](https://code.claude.com/docs/en/desktop#coming-from-the-cli) / [feature-availability](https://code.claude.com/docs/en/feature-availability#availability-by-model-provider) / [llm-gateway-connect](https://code.claude.com/docs/en/llm-gateway-connect#configure-each-surface) ほか2箇所

**Bedrock / AWS**

- **v2.1.206 更新ログ追加**: Amazon Bedrock で `awsCredentialExport` ヘルパーを使う際、外向き通信が制限されたネットワークで起動が数分間ハングする不具合が修正された。 — [English](https://code.claude.com/docs/en/changelog)

### 📝 ドキュメント改善 — 1件

- **エラー索引への 4 件追加**: エラーリファレンス冒頭の索引表に、ダウンロードのタイムアウト、`--json-schema` の不正、Claude Desktop からのサーバーインポート失敗、信頼できないマーケットプレイスの 4 行が追加され、それぞれの解説セクションへ誘導するようになった。 — [English](https://code.claude.com/docs/en/errors#find-your-error)

### 今回変更のなかったカテゴリ

**❓ 未分類（語彙マッチなし・要 LLM 裁定）**

## 分類の内訳（付録）

LLM（Opus）による意味分類と、語彙マッチによる機械分類（参考）の対比。「分割」= 1 つの changelog 等をカテゴリ別に分けたエントリ。

| エントリ | LLM 分類 | 機械分類（参考） | 統合項目数 | 分割 |
|---|---|---|---:|:--:|
| agent-view#Monitor sessions with agent view ほか | サブエージェント, セッション・コンテキスト | サブエージェント, セッション・コンテキスト | 3 |  |
| debug-your-config#See what loaded into context ほか | コマンド | MCP・外部連携, エンタープライズ基盤, コマンド, セッション・コンテキスト, パーミッション・セキュリティ, モデル・利用状況, 未分類（語彙マッチなし・要 LLM 裁定）, 設定・環境変数 | 16 |  |
| claude-code-on-the-web#Work with sessions ほか | コマンド, アプリ・プラットフォーム | アプリ・プラットフォーム, セッション・コンテキスト | 3 |  |
| cli-reference#CLI flags | コマンド | コマンド | 1 |  |
| errors#Command-line errors | コマンド | 拡張機能（プラグイン・スキル・フック） | 1 | ✔ |
| auto-mode-config#Define trusted infrastructure | パーミッション・セキュリティ | パーミッション・セキュリティ | 1 |  |
| permission-modes#Analyze before you edit with plan mode | パーミッション・セキュリティ, セッション・コンテキスト | パーミッション・セキュリティ | 1 |  |
| desktop-quickstart#Start your first session ほか | パーミッション・セキュリティ | アプリ・プラットフォーム, セッション・コンテキスト, パーミッション・セキュリティ | 7 |  |
| desktop ほか | アプリ・プラットフォーム | アプリ・プラットフォーム | 4 |  |
| desktop#Work with code ほか | アプリ・プラットフォーム, パーミッション・セキュリティ | アプリ・プラットフォーム, パーミッション・セキュリティ | 2 |  |
| desktop#Coming from the CLI? ほか | エンタープライズ基盤, アプリ・プラットフォーム | アプリ・プラットフォーム, エンタープライズ基盤 | 5 |  |
| errors#Find your error | ドキュメント改善 | 拡張機能（プラグイン・スキル・フック） | 1 |  |
| errors#Installation errors ほか | アプリ・プラットフォーム | アプリ・プラットフォーム, エンタープライズ基盤 | 2 |  |
| errors#Command-line errors ほか | MCP・外部連携 | MCP・外部連携, 拡張機能（プラグイン・スキル・フック） | 2 | ✔ |
| mcp#Installing MCP servers | MCP・外部連携 | MCP・外部連携 | 1 |  |
| errors#Request errors ほか | モデル・利用状況 | モデル・利用状況 | 4 |  |
| fast-mode#Toggle fast mode ほか | モデル・利用状況 | モデル・利用状況 | 2 |  |
| headless#Examples ほか | ワークフロー, Agent SDK | Agent SDK, 拡張機能（プラグイン・スキル・フック） | 2 |  |
| debug-your-config#Check hooks | 拡張機能（プラグイン・スキル・フック）, 設定・環境変数 | 設定・環境変数 | 1 |  |
| plugin-dependencies#How constraints interact ほか | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 3 |  |
| plugin-marketplaces#Marketplace schema | 拡張機能（プラグイン・スキル・フック）, パーミッション・セキュリティ | 拡張機能（プラグイン・スキル・フック） | 1 |  |
| settings#Settings files ほか | 拡張機能（プラグイン・スキル・フック）, 設定・環境変数 | 拡張機能（プラグイン・スキル・フック） | 3 |  |
| skills#Troubleshooting | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |  |
| plugins-reference#Plugin components reference | 拡張機能（プラグイン・スキル・フック） | 拡張機能（プラグイン・スキル・フック） | 1 |  |
| statusline#Subagent status lines | 拡張機能（プラグイン・スキル・フック）, サブエージェント | 拡張機能（プラグイン・スキル・フック） | 1 |  |
| sub-agents#Configure subagents ほか | サブエージェント | サブエージェント | 2 |  |
| worktrees#Start Claude in a worktree ほか | 拡張機能（プラグイン・スキル・フック）, ワークフロー | 拡張機能（プラグイン・スキル・フック） | 2 |  |
| worktrees#Clean up worktrees | ワークフロー | ワークフロー | 1 |  |
| agent-sdk/agent-loop#Control how the loop runs | Agent SDK | Agent SDK | 1 |  |
| agent-sdk/typescript#Types ほか | Agent SDK | Agent SDK, MCP・外部連携 | 2 |  |
| agent-sdk/typescript#Tool Input Types | Agent SDK | Agent SDK | 1 |  |
| changelog | コマンド | MCP・外部連携 | 1 | ✔ |
| changelog | パーミッション・セキュリティ | MCP・外部連携 | 1 | ✔ |
| changelog | エンタープライズ基盤 | MCP・外部連携 | 1 | ✔ |
| changelog | エンタープライズ基盤 | MCP・外部連携 | 1 | ✔ |
| changelog | サブエージェント | MCP・外部連携 | 1 | ✔ |
| changelog | MCP・外部連携 | MCP・外部連携 | 1 | ✔ |
| changelog | モデル・利用状況 | MCP・外部連携 | 1 | ✔ |
| changelog | 設定・環境変数 | MCP・外部連携 | 1 | ✔ |
| changelog | アプリ・プラットフォーム | MCP・外部連携 | 1 | ✔ |
| changelog | ワークフロー | MCP・外部連携 | 1 | ✔ |
| changelog | 拡張機能（プラグイン・スキル・フック） | MCP・外部連携 | 1 | ✔ |

<!--
base_commit: c5b9ea9
head_commit: 191b578
generator: build_category_summary.py render --entries (trial v3)
-->
